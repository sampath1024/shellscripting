name: practice
on: 
  workflow_dispatch:

jobs:
   testing:
     runs-on: ubuntu-latest


     steps:
       - name: Checkout Repository
         uses: actions/checkout@v4


       - name: testing shell
         run: |
             sh Parameters.sh
            
           
    
     
   
   
    
